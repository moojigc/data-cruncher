import path = require('path');

/**
 * Relative path to the python binary in the virtual environment.
 */
export const REL_PYTHON_PATH = path.join('server', '.venv', 'bin', 'python3');
export const REL_SERVER_DIR = 'server';
